import { Sparkles } from "lucide-react";
export function AuthCard({ title, description, children }: { title: string; description: string; children: React.ReactNode }) { return <main className="grid min-h-screen place-items-center p-5"><section className="w-full max-w-md"><div className="mb-7 flex items-center justify-center gap-2 text-xl font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-brand text-white"><Sparkles size={18}/></span>VivaData</div><div className="card p-7"><h1 className="text-2xl font-bold">{title}</h1><p className="muted mb-6 mt-2">{description}</p>{children}</div><p className="muted mt-5 text-center text-xs">Inteligência responsável baseada apenas em dados públicos.</p></section></main>; }
