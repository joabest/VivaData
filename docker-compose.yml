services:
  postgres:
    image: postgres:16-alpine
    environment: { POSTGRES_USER: vivadata, POSTGRES_PASSWORD: vivadata, POSTGRES_DB: vivadata }
    ports: ["5432:5432"]
    volumes: ["postgres_data:/var/lib/postgresql/data"]
volumes: { postgres_data: {} }
